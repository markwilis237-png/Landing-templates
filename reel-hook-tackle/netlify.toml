# Netlify config for the Reel Hook Tackle site specifically.
# Intended for a dedicated Netlify site whose "Base directory" is set to
# reel-hook-tackle/ (see README.md "Netlify setup" section) — NOT for the
# repo-root site, which serves the unrelated Apex Mobile Detailing template.

[build]
  # No build step — plain static HTML/CSS/JS.
  publish = "."
  command = ""
